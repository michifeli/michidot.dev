---

---

<style>
  .building-page {
    min-height: calc(100vh - var(--header-h));
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 40px 20px;
    color: var(--muted);
  }
  .building-box {
    text-align: center;
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.01),
      rgba(255, 255, 255, 0.005)
    );
    padding: 28px 36px;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.02);
    box-shadow: 0 8px 24px rgba(2, 6, 23, 0.6);
  }
  .building-title {
    font-size: 1.6rem;
    color: #e6eef8;
    font-weight: 700;
    margin-bottom: 8px;
  }
  .building-sub {
    color: var(--muted);
    font-size: 0.98rem;
  }
</style>

<main class="building-page">
  <div class="building-box">
    <div class="building-title">construyendo...</div>
    <div class="building-sub">
      Esta sección está en construcción. Vuelve pronto.
    </div>
  </div>
</main>
